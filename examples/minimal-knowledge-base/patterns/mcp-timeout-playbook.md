# MCP 超时与重试排查清单

适用于：MCP、HTTP 工具、代理或慢网络环境下的偶发超时。

## 快速检查

1. 超时发生在客户端还是服务端（日志时间戳对比）。
2. 请求体积与单次返回行数是否过大。
3. 是否存在 DNS/TLS 握手阶段的尖刺延迟。

## 常见修复

- 增大客户端 read timeout；为长任务改为流式或分页。
- 读请求：有限次指数退避重试；写请求：避免盲重试，使用幂等键。
- 拆分大 payload；缓存稳定的热点查询结果。

## 反模式

- 无退避的 tight loop 重试。
- 在超时原因未明时盲目调大 server 线程池。
